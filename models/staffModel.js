
class Staff {
  constructor(id, name, lastName, email, role) {
      (this.id = id),
      (this.name = name),
      (this.lastName = lastName),
      (this.email = email),
      (this.role = role);
  }
}

export { 
  Staff
};
